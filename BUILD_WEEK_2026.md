# Ensemble AI — Build Week 2026 技術記録

この文書は、OpenAI Build Week 2026における実装範囲、再現手順、検証結果、GPT-5.6とCodexの使い方を記録する技術資料です。申請時の紹介文ではなく、審査・監査・デモ準備で確認できる事実をまとめています。

## 担当と期間

- プロジェクト: Ensemble AI（既存のCostume Coordinatorを拡張）
- 対象部門: Apps for Your Life
- Build Week開始: 2026-07-13
- 提出期限: 2026-07-21 17:00 PDT（日本時間 2026-07-22 09:00）
- PM・相談担当: GPT-5.6 Sol
- 実装担当: GPT-5.6 Luna
- 検証・レビュー: GPT-5.6 Sol / Luna
- コア機能を構築したCodex task: このタスク（提出時に`/feedback`でSession IDを取得）

## GPT-5.6とCodexを使用した箇所

GPT-5.6は、このCodex task内の開発工程で使用しました。公開アプリの実行時にGPT-5.6やOpenAI APIを呼び出す構成ではありません。

| 担当 | 開発工程での役割 |
| --- | --- |
| GPT-5.6 Sol | PM、要件整理、Build Week要項の解釈、製品方針・プライバシー・セキュリティ判断、検証レビュー |
| GPT-5.6 Luna | 実装担当としてリポジトリ監査、設計、React / TypeScript / Workerの変更、テストと技術記録の作成 |
| Codex | ファイル調査・編集、コマンド実行、テスト、ビルド、既存画面のブラウザ／モバイル確認、Gitによる変更追跡（新UIの最終確認は公開前チェック） |
| 人間 | プロダクト目的、コストを発生させない実行方式、AIに最終決定を委ねない方針、公開範囲と提出内容の最終判断 |

申請文とデモ動画では、GPT-5.6が「アプリ内で写真を解析している」とは説明しません。正確な説明は、GPT-5.6 Sol / LunaをCodexで相談・実装・検証に使用し、その成果として無料の端末内解析を強化した、というものです。OpenAI API連携、実行時のモデル推論、モデルによる選定理由生成は実装・申請上の機能として主張しません。

Build Weekの公式案内はCodexとGPT-5.6の使用、およびデモ動画で両者の使い方を説明することを求めています。公式FAQもGPT-5.6の使用を必須としています。本プロジェクトでは、上記の開発工程がその使用箇所です。

- [Official Rules](https://openai.devpost.com/rules)
- [Build Week FAQ](https://openai.devpost.com/details/faqs)

## 2026-07-13より前から存在した機能

Git履歴上、Build Week直前のHEADは`694a5b1`（2026-06-27）です。次の機能はBuild Week開始前から存在していました。

- React / TypeScript / ViteによるPWA
- 個人ワードローブへの衣装写真、色、トーン、柄、衣装種類の登録と手入力
  - ドレス、スーツだけでなく、ワイシャツ、ネクタイ、蝶ネクタイ、小物・アクセサリーも単品で登録可能
- 所有衣装の検索・一覧表示
  - 日本語キーワード、色系統、柄、トーン、季節、種類、シルエット、スーツ属性による検索は`be14d8b`と`42fd112`（2026-06-26）で追加済み
- ブラウザCanvasによる主色・副色・トーンのローカル解析
- イベントと衣装テーマの作成
- 招待URLからの参加、候補衣装のオンライン提出
- Cloudflare Workers / D1 / R2によるイベント、参加者、衣装属性、提出写真の一時保管
- テーマ、希望順位、使用履歴、グループ調和を使う決定的最適化
- 主催者向けの最適化スコアと選定理由
- PWAのオフライン閲覧、Google Drive / Dropboxへの任意バックアップ

## Build Weekで追加・強化した機能

- お気に入りコーデ（完成した装い）
  - 登録済みのベース衣装と1〜2点の構成品を、名前付きの組み合わせとして作成・編集・削除
  - 例: スーツ＋ワイシャツ＋ネクタイ、ドレス＋小物、ブラウス＋スカート＋ブローチ
  - ブラウス、その他のトップス、スカート、パンツ・ズボンを衣装種類へ追加
  - 写真必須、上衣・下衣・ネクタイ類は各1点まで、トップスには下衣を1点必須とするルールを保存前に検証
  - 構成品を削除するときは依存するコーデを明示し、確認後にそのコーデだけを同時削除
  - 構成品すべての写真を並べて確認できるプレビューと、所有衣装／コーデを横断する検索
  - 既存検索ロジック自体を新機能とは数えず、Build Week差分はコーデ検索、タグ保存・検索、表示・アクセシビリティ改善として区別
- 登録アイテムからの自動コーデ提案
  - 有効なお気に入りコーデがないベース衣装だけを対象に、登録済みの上衣・下衣・小物から完成候補を端末内で生成
  - スーツ＋シャツ＋小物、ドレス＋アクセサリー、トップス＋ボトムス＋小物の3種類を別ルールで構成
  - 色、トーン、柄の競合、着用季節、タグを決定的に採点し、短い根拠と「組み合わせやすさ」を表示
  - 1ベース衣装につき最大3候補、全体最大12候補の上限とラウンドロビンで、組合せ爆発と特定衣装への偏りを防止
  - 同じシャツや小物だけに上位候補が集中しないよう、点数1位の後は構成品を共有しない代替候補を優先
  - 提案は未保存でもイベント候補に使え、よく使う組み合わせは写真と構成を確認・改名してからお気に入りへ保存可能
  - 提案IDは`favorite-outfit:auto-<ownerHash>-<rank>`の固定スロットとし、小物を差し替えてもサーバー上で同じ候補を更新
- イベント候補を「単一の衣装ID」ではなく「完成した装い」で扱う変換層
  - 有効なお気に入りと自動提案を、既存最適化が扱える1つの仮想`Costume`候補へ決定的に変換
  - トップス、ボトムス、ネクタイ、蝶ネクタイ、小物・アクセサリーの単品をイベント候補から除外
  - 有効なコーデを持つスーツは単品スーツ候補を抑制し、スーツだけが選ばれる状態を防止
  - 構成元ID・名前・写真を保持し、候補・提出・結果画面では複数写真を1組として表示
  - 決定時の使用履歴は仮想候補IDだけでなく物理的な全構成品へ記録し、いずれか1点が直近使用済みなら同じコーデを除外
  - 同じ小物を含む別の仮想候補も構成元IDで排他し、1つのブローチ等を2人へ同時割当しない
- オンライン提出の完成コーデ対応
  - PWAから構成元ID・名前の順序付き配列を送信し、Workerで長さ、重複、文字数、`sourceCostumeId`との整合を検証
  - D1の`components_json`に構成品メタデータ、R2に`componentIndex`と対応する写真を保存
  - 通常の再送では既存写真スロットを先に確認し、途中で通信が失敗しても未送信スロットだけを再試行
  - 同じローカル候補の構成品や属性を編集した場合は、本人所有のサーバー候補だけを更新し、旧写真スロットを破棄して全構成写真を再提出
  - 構成候補の`updatedAt`をリビジョンとして検証済み`components_json`へ保持し、構成IDや属性が同じまま写真だけを差し替えた場合も古いR2スロットを再利用しない
  - 再提出前に、参加者本人が以前提出した自動提案のうち現在の候補から外れたものだけを、認証付きWorker経路でR2写真とD1行から冪等に整理
  - 全構成品の写真が揃うまで提出済み・最適化候補にしない
  - 旧Workerは機能上限を返さないため、単品提出は継続しつつ完成コーデ提出だけを安全な説明付きで停止
  - `components=[]`の旧衣装は従来どおり単品候補として読み込む後方互換
- 参加者が招待フロー内から衣装写真を登録し、元の提出画面へ安全に戻れる専用導線
- Canvas解析の精度改善
  - 写真中央の衣装領域を優先
  - 四隅で共通する背景色、白・黒・暖色系ニュートラル背景を抑制
  - 近い色を量子化して主色候補の偏りを軽減
  - 色の主・副候補とトーンを既存フォームへ入力
- 画素の境界、方向性、分散、連結領域を使った保守的な柄候補
  - 対象は無地、ストライプ、チェック、ドット、その他
  - confidence、警告、判定困難項目を返す
  - 衣装種類、シルエット、フォーマル度は写真から推定しない
- 未解析、確認中、入力済み、要確認、失敗、手動入力、再確認のUI状態
- 入力候補を利用者が確認・修正してから保存するレビュー導線
- JPEG / PNG / WebP、5MB以下という画像選択時の検証
- オンライン提出用WorkerでのMIME、magic bytes、実データ量の再検証と上限付きstream読込
- R2画像レスポンスの`private, no-store`、`nosniff`、旧SVGメタデータの安全な扱い
- 管理者トークンをURLクエリへ含めず、ヘッダーで送るようにするクライアント修正
- 参加者向け結果画面への、既存の決定的最適化結果から作る短い選定理由
- 低confidence、画像処理失敗、未対応形式でも手入力を続けられるフォールバック
- プライバシーポリシーと利用規約への端末内解析・オンライン提出境界の明記
- 端末内解析UI、画像検証、参加導線、選定理由、Worker画像境界、既存最適化のテスト

## 実行時アーキテクチャ

```text
個人ワードローブ（IndexedDB）
  ├─ 衣装写真を選択
  │    └─ 形式・容量検証 → Canvas端末内解析
  │         └─ 主色 / 副色 / トーン / 保守的な柄候補
  │              └─ 利用者が確認・修正して単品衣装を保存
  ├─ お気に入りコーデ
  │    └─ ベース衣装ID ＋ 構成品ID（1〜2点）を保存
  └─ 保存済みコーデがないベース衣装
       └─ 色 / トーン / 柄 / 季節 / タグで端末内自動提案
            └─ イベント候補作成時に1つの仮想Costumeへ変換
                 ├─ 単品で完成する衣装候補
                 └─ 保存済み／自動提案の完成コーデ候補
                      （構成ID・名前・写真を保持）
                      ├─ オフラインJSON提出
                      └─ オンライン提出
                           ├─ D1: 衣装属性 + components_json
                           └─ R2: componentIndex順の写真スロット
                                └─ 主催者が取り込み
                                     └─ 既存の決定的最適化
                                          └─ 1人につき1組 + 決定的理由
```

端末内解析はブラウザのCanvasとTypeScriptの決定的な画素処理だけで完結します。写真解析のための外部AI通信、OpenAI API呼び出し、モデル推論、サーバー側解析はありません。したがってOpenAI APIキーやモデル実行用のデプロイ設定も必要ありません。

保存済みと自動提案のコーデは、新しい最適化エンジンを追加せず、既存エンジンへの入力境界で仮想`Costume`へ変換します。最大3点（ベース衣装＋2点）を1候補とし、色・季節・タグは構成品から統合、トーン・種類・シルエット・スーツ詳細はベース衣装から継承します。柄は小物・ネクタイ類、下衣、上衣、ベース衣装の順で無地以外を優先する決定的な規則です。保存済みIDは`favorite-outfit:<combinationId>`、自動提案IDは`favorite-outfit:auto-<ownerHash>-<rank>`とし、構成元のID・名前・写真は別フィールドで保持します。

自動提案は外部サービスの「解析」ではなく、登録済み属性を使うTypeScriptの決定的な候補生成です。実行ごとのトークン費用は発生しません。有効なお気に入りがあるベースでは保存内容を優先し、未設定または構成欠損時だけ自動候補を作ります。組み合わせやすさの点数と理由は構成候補の説明であり、グループ最終割当の理由とは分離しています。

完成コーデが決定したときは、使用履歴を仮想候補IDではなくベース衣装・シャツ・スカート・ネクタイ等の全構成元IDへ記録します。次のイベントでは、構成品のどれか1点でも設定期間内に使用済みなら、その完成コーデを候補から外します。これにより、同じ小物を別コーデ経由で連続使用する抜け道を作りません。

イベント候補では、トップス、ボトムス、ネクタイ、蝶ネクタイ、小物・アクセサリーの単品を除外します。単品で成立する衣装は従来どおり候補にでき、有効な保存済みまたは自動提案があるスーツは単品候補を抑制します。トップスは下衣が写真付きで揃った完成候補としてだけ扱います。この変換により、最適化エンジンは引き続き1人につき1つの候補IDを選びますが、そのIDが表す対象は「1着」ではなく「単品または完成した装い1組」です。

最適化では仮想候補IDに加え、その候補が持つ物理構成IDも割当済み集合へ登録します。そのため、別名のコーデでも同じスカートやブローチを複数人へ同時割当しません。オンライン提出のローカルIDは別端末間で偶然一致し得るため、取り込み候補は提出者名でスコープし、別人の所有品を誤って同一物と見なしません。

オンライン提出を選んだ場合は、利用者が保存した後に既存のCloudflare Worker経路で衣装属性と構成品メタデータをD1、各構成品の写真をR2へ一時保存します。写真は`componentIndex`と`photos.sort_order`を対応させ、通常の再送では既存スロットを先に返します。提出状態と主催者取り込みは全スロットが揃っているかで判定します。これはイベント共有機能の通信であり、写真解析のための送信ではありません。

移行前からある行は`components=[]`として単品衣装のまま扱います。旧Workerは`maxOutfitComponents`を返さないため、PWAは完成コーデのオンライン提出を試さず、更新が必要であることを表示します。既存の単品提出、手入力、オフラインJSON経路は維持されます。

最終的な衣装割り当ては、Build Week前からある決定的最適化エンジンが担当します。Build Weekで追加した短い理由も、そのエンジンの既存理由から最大3件を整形したもので、文章生成モデルは使用しません。同じ入力からは同じ割り当てと理由を再現できます。

## セットアップ

### PWA

```bash
pnpm install
pnpm dev
```

端末内の写真解析だけを試す場合、OpenAIアカウント、OpenAI APIキー、追加の環境変数は不要です。

オンライン提出もローカルで試す場合は、ルートの`.env`に既存WorkerのURLを設定します。

```dotenv
VITE_EVENT_API_URL=https://costume-coordinator-events.YOUR_ACCOUNT.workers.dev
```

`.env`やローカル用の変数ファイルはGitへ追加しません。

### OpenAI APIキー

設定手順はありません。公開PWA、Cloudflare Worker、テストのいずれもOpenAI APIを呼ばず、`OPENAI_API_KEY`や同等のsecretを必要としません。APIキーをブラウザ用の`VITE_*`変数へ入れたり、Cloudflare Workerへ審査用として仮登録したりしません。GPT-5.6 Sol / LunaはCodex内の開発工程で使用し、アプリ実行時の機能と混同しないようにします。

### Cloudflare Worker

```bash
cd workers/event-api
pnpm install
pnpm exec tsc --noEmit
```

Workerはオンライン提出、参加者認証、D1 / R2への一時保存に使用します。OpenAI用secretの登録は不要です。Cloudflareへのデプロイ認証情報や既存bindingは、リポジトリ外の安全な設定で管理します。

完成コーデ対応では、既存D1へ次の追加マイグレーションを適用します。`schema.sql`は新規D1用です。既存D1に対して`schema.sql`だけを再実行しても既存テーブルへ列は追加されないため、追加マイグレーションを省略しません。

```bash
cd workers/event-api

# 新規ローカルD1だけに使用
pnpm run db:migrate:local

# 既存ローカルD1
pnpm exec wrangler d1 execute costume-events --local \
  --file=./migrations/20260717_add_outfit_components.sql
```

本番D1では、対象データベースを確認してからマイグレーションを1回だけ適用します。追加対象は`costumes.components_json`列だけです。本番データの削除、既存写真行の書き換え、全体リセットは行いません。

```bash
cd workers/event-api

pnpm exec wrangler d1 execute costume-events --remote \
  --file=./migrations/20260717_add_outfit_components.sql

pnpm exec tsc --noEmit
pnpm exec wrangler deploy
```

デプロイ順は次のとおりです。

1. 対象D1、R2 binding、`ALLOWED_ORIGINS`、容量上限を確認する。
2. D1へ追加マイグレーションを1回だけ適用する。
3. Workerをデプロイする。
4. `GET /api/health`で`apiVersion: "2026-07-18.2"`と`uploadLimits.maxOutfitComponents: 3`を確認する。
5. 単品提出と完成コーデの3スロット提出を専用デモイベントで確認する。
6. PWAの型チェック、テスト、ビルド後にフロントエンドを公開する。

列追加前に新Workerを出すと`components_json`参照が失敗するため、Workerを先にデプロイしません。PWAを最後にすることで、未更新Workerへ完成コーデを送る時間を作りません。マイグレーションは加算的で、既存行は既定値`[]`により単品衣装として後方互換です。

### 既知の制約

今回の安全な本番移行では、未監査の既存写真行によってマイグレーションが失敗することを避けるため、`(costume_id, sort_order)`の一意インデックスを追加していません。画面からの通常操作や直列の再試行は、Workerが既存`componentIndex`を先に検索するため重複しません。ただし、同じ参加者が同一の空スロットへ完全に同時のPOSTを送った場合は、競合して2行入る可能性が残ります。現行UIは1送信を直列実行し、送信中の再操作を抑止します。本番写真行を監査してから、重複解消と一意インデックス追加を別の保守作業として検討します。

自動提案はベース衣装と順位の固定IDを使うため、構成品の差し替えでD1候補が増殖することを防ぎます。提案数が減る、ベース衣装を削除する、またはお気に入りへ切り替える場合は、PWAが現在有効な自動提案IDをWorkerへ送ります。Workerは参加者トークンを検証し、その本人が所有する`favorite-outfit:auto-`行のうち一覧にないものだけを対象に、R2オブジェクトを先に削除してからD1写真・衣装行を冪等に削除します。単品衣装、保存済みコーデ、他参加者、イベント全体はこの同期の対象外です。旧Workerでこの同期が必要な場合は写真アップロード前に更新要求を表示し、既存の単品提出だけで同期対象がない場合は旧経路を維持します。

オフラインJSONは後方互換のため各候補にBase64写真を含めます。自動提案は1ベース3候補・全体12候補で打ち止めますが、高解像度写真と候補数が多い場合はファイルが大きくなります。本番の通常導線はCloudflareのオンライン提出であり、オフラインJSONはAPI未設定時のフォールバックです。

## テストと検証

```bash
# ルート
pnpm check
pnpm test
pnpm build

# Worker
cd workers/event-api
pnpm exec tsc --noEmit

# 差分の空白・競合確認
git diff --check
```

重点的に確認する項目は次のとおりです。

- 主色抽出における中央衣装領域、背景候補の除外、色量子化
- 柄候補のconfidence、警告、判定困難時の保守的な戻り値
- 成功、低confidence、失敗、手動入力、再確認と保存前確認
- JPEG / PNG / WebP、空ファイル、5MB超過、MIMEとmagic bytesの不一致
- `Content-Length`がないchunked uploadでも上限を超えた時点で中断すること
- 端末内解析の写真を外部AIへ送らないというUI表示
- 参加者の招待トークンを保持した追加・復帰導線
- 選定理由の整形、保存、公開結果での復元
- お気に入りコーデの入力制約（ベース衣装＋1〜2点、写真必須、上衣／下衣／ネクタイ類の重複防止）
- 完成コーデへの属性集約、構成写真順、検索、欠損構成品の除外
- お気に入り未設定時のスーツ＋シャツ＋小物、ドレス＋小物、ブラウス＋スカート＋小物の自動提案
- 自動提案のワードローブ順非依存、固定スロットID、手動保存優先、候補上限、写真欠損時の安全な除外
- 候補上限内での構成品多様性と、物理構成品排他後にも代替候補が残ること
- トップス・ボトムス・小物単品の候補除外と、構成品を共有する仮想候補の同時割当防止
- 完成コーデ決定時の全構成品への使用履歴記録と、構成品単位の直近使用除外
- トップス、ボトムス、ネクタイ、蝶ネクタイ、小物単品の候補除外と、旧単品衣装の後方互換
- オフラインJSONで未提出のお気に入り下書きを含めず、提出した構成メタデータだけを保持すること
- Workerでの構成品スキーマ検証、予約済みID接頭辞、参加者所有権、属性変更時の安全な写真リセット
- 構成リビジョンの非負整数検証、写真だけの差し替え時の全スロット更新、revision省略の旧クライアント互換
- 古い自動提案のactive ID検証、本人所有権、対象接頭辞、R2 / D1冪等削除、旧Worker・単品提出互換
- `componentIndex`ごとの直列再送、部分再送、全スロット完成前の提出済み判定禁止
- 主催者取り込み時の構成ID・名前・写真順の復元と、不完全コーデの候補除外
- 既存の決定的最適化への回帰がないこと
- 管理者トークンがURLへ入らないこと

`c334203`までのBuild Week第1段階では、2026-07-17にVite/PWAの型チェック、53ファイル・359件の全テスト、本番ビルド、Worker型チェック、`git diff --check`が成功しています。モバイルは390×844と320×700で横スクロールがなく、主要フォーム操作が44px以上であることを確認しました。

お気に入り完成コーデを追加したBuild Week第2段階では、2026-07-17にルートの`npm run check`、55ファイル・400件の`npm test`、`npm run build`、Workerの`npx tsc --noEmit`、`git diff --check`がすべて成功しました。このうち完成コーデの作成・候補化・使用履歴・オンライン提出・Worker境界・取り込み・後方互換を直接扱う対象テストは10ファイル・72件で、すべて成功しています。Workerセキュリティ境界は24件すべて成功し、新規`schema.sql`と追加マイグレーションのローカルSQLite実行も成功しました。

登録アイテムからの自動コーデ提案、ブラウス／スカート等の上下別衣装、構成品排他、写真リビジョンを追加した`d055d37`では、2026-07-18に`npm run check`、56ファイル・417件の`npm test`、`npm run build`、Workerの`npx tsc --noEmit`がすべて成功しました。直接変更した自動提案、検索／表示、提出、最適化、Worker境界の主要6テストファイルは合計75件、Workerセキュリティ境界は25件すべて成功しています。主な対象は`tests/favorite-combinations.test.ts`、`tests/costume-search-filter.test.ts`、`tests/outfit-ui-presentation.test.ts`、`tests/system-optimizer.test.ts`、`tests/submit-participant-costumes.test.ts`、`tests/event-api-security.test.ts`です。

候補多様性の`01bfb72`、`78befbe`、`23aff8a`と、古い自動提案を本人の提出だけから整理する`f85c67f`を加えた最終状態では、2026-07-18に`npm run check`、56ファイル・424件の`npm test`、`npm run build`、Workerの`npx tsc --noEmit`、`git diff --check`が成功しました。上記の主要6テストファイルは合計82件、Workerセキュリティ境界は28件、オンライン提出ユーティリティは10件すべて成功しています。9着のスーツと9枚のシャツが同属性でも、第1候補が9枚へ分散して物理排他後の割当可能性を保つ回帰ケースを含みます。

ビルドには既存のdynamic/static import重複と500kB超chunkの警告がありますが、失敗ではありません。2026-07-18には本番と分離したローカルプロファイルで、7点の合成衣装登録、写真候補入力、スーツ／シャツ／ネクタイのお気に入り保存、ブラウス／スカート／ブローチを含む自動提案、3人分の決定的最適化、構成写真と理由の結果表示まで実画面で確認しました。この確認では公開Worker、D1、R2、本番利用者データを使用していません。1280×720の実画面から日本語ナレーション・英語字幕付き2分23.96秒の提出動画を作成し、H.264 / AACの全編デコードとブラウザ再生も確認済みです。再生成と提出手順は[`BUILD_WEEK_VIDEO.md`](./BUILD_WEEK_VIDEO.md)に記録しています。

## 3分未満のデモ手順

完成済み動画、正確なナレーション、英語字幕、再生成コマンド、YouTube提出手順は[`BUILD_WEEK_VIDEO.md`](./BUILD_WEEK_VIDEO.md)を参照してください。

本番利用者の画像やイベントを使わず、分離したローカルブラウザプロファイル、IndexedDB内の専用イベント、人物を含まない合成衣装画像を使いました。完成動画の最終結果は次の3人です。

- Demo A: 青系Aラインドレス
- Demo B: ネイビースーツ＋白ワイシャツ＋ボルドーネクタイの保存済み「演奏会の定番」
- Demo C: アイボリーブラウス＋ネイビースカート＋ゴールドブローチの自動提案

動画は、完成結果の先出し、Build Week前後の区別、写真選択、端末内候補入力、利用者の確認、自動提案、お気に入り保存、3人の最適化結果、調和スコア、GPT-5.6とCodexの開発時の役割、検証結果の順に構成しています。実画面は1280×720で撮影し、人物、位置情報、招待／管理者トークン、本番イベントを含めていません。正確な14シーン、ナレーション、焼き込み英語要約、完全英訳SRTは[`tools/build-week-video/scenes.json`](./tools/build-week-video/scenes.json)と[`BUILD_WEEK_VIDEO.md`](./BUILD_WEEK_VIDEO.md)に記録しています。

デモの音声では、GPT-5.6 SolがPM・相談・製品／セキュリティ判断、GPT-5.6 Lunaが実装担当、Sol / Lunaが検証・レビューであり、Codexが監査・コード編集・テスト・ブラウザ確認・Git作業を進めたことを説明します。アプリ実行時の写真候補入力は端末内の決定的処理、最終割り当ては既存の決定的最適化であり、OpenAI APIや実行時モデル推論ではないことを明示します。

完成動画の撮影では公開Worker、D1、R2へサンプルを送りませんでした。本番データの削除、既存イベントの変更、全体リセットを行うデモ機能やseed処理も追加していません。撮り直す場合は分離したローカルプロファイルで別名の専用イベントを作ります。オンライン提出そのものは、Worker公開後に専用デモ参加者で別途スモークテストします。

## プライバシーとセキュリティ

- 写真解析は端末内で完結し、解析目的で外部AIサービスへ写真を送信しない。
- OpenAI APIを使用しないため、OpenAI APIキーを設定・配布・コミットしない。
- 解析候補はフォーム上の一時値であり、利用者が確認・修正して保存する。
- 解析に失敗しても既存フォームと手入力を無効化しない。
- ローカル利用では衣装データとお気に入りコーデの構成IDをIndexedDBに保存し、Google Drive / Dropbox同期は利用者が選んだ場合だけ行う。
- 自動コーデ提案も端末内の登録属性だけで都度生成し、提案のために写真や衣装属性を外部AIへ送信しない。
- お気に入りコーデの作成だけではCloudflareへ写真を送らない。利用者がオンラインイベントへ提出した構成品だけを送る。
- オンライン提出では、Cloudflare Workerが参加者・管理者トークンとイベント期限を確認する。
- オンライン提出写真はJPEG / PNG / WebPのMIME、magic bytes、空ファイル、実データ量をWorkerで再確認する。
- 完成コーデの構成配列は最大3点、重複なし、長さ制限付きで検証し、予約済みの仮想候補IDと一致しない入力を拒否する。
- 構成品ごとの写真は順序付きスロットへ保存し、途中提出を「提出済み」と扱わない。
- R2画像は認証付きWorker経路で配信し、レスポンスをprivateかつno-storeにする。公開バケットURLを前提にしない。
- オンライン提出データはイベントの保存期限まで一時保管し、期限後または管理者による削除時にD1 / R2から削除する。
- 画像、data URL、認証トークン、秘密情報をログへ出力しない。
- GitHubが公開される前提で、`.env`、`.dev.vars`、秘密鍵形式を継続的に走査する。
- OpenAI API、実行時モデル切替、有料課金・請求処理は実装しておらず、OpenAI用secretを設定しない。

今回の差分、作業ツリー、ビルド成果物、Git全履歴を値を表示しない方式で再走査しました。OpenAI、GitHub、AWS、Stripe、Slack形式の高確度な秘密鍵は検出されず、今回の差分と現在の作業ツリーにはGoogle APIキー形式もありません。Git全履歴では、次項のBuild Week以前に追加・削除された1つのFirebase Web APIキー形式の値が、履歴上の2変更で検出されました。

Build Week以前のGit履歴にはFirebase Web APIキー形式のクライアント識別子があります。Firebase Web APIキーは一般にブラウザへ配布される識別子ですが、安全性はキー単体ではなくGoogle Cloud側の制限とFirebase Security Rulesに依存します。本番公開前にHTTP referrer、利用API制限、Security Rulesを外部設定で確認し、意図しないProjectであれば無効化またはローテーションします。この値は技術記録やログへ転載しません。

## Codexが実装を加速した箇所

- 既存のイベント、参加者、R2、衣装モデル、最適化経路の並列監査
- Build Week前の機能と追加差分のGit履歴による切り分け
- 画素統計を使う端末内解析アルゴリズムとレビューUIの実装
- 単品、小物、完成コーデのドメイン境界を既存型・検索・最適化から追跡し、仮想候補への変換層を実装
- スーツ・ドレス・上下別衣装の構成規則、決定的な相性スコア、固定候補スロット、保存済み優先を一貫して実装
- Reactのコーデ作成・検索・複数写真UIと、D1 / R2の順序付き構成品スロットを一貫して更新
- React、共有型、Cloudflare Workerをまたぐ導線とセキュリティ修正
- 構成欠損、途中再送、旧Worker、旧単品データを含む異常系・保存境界・最適化回帰テスト
- 既存画面の実ブラウザでのモバイル幅、操作サイズ、コンソール確認（新UIの最終確認は公開前チェック）
- 公開GitHubを前提とした秘密情報境界とGit履歴の監査
- デモ・セットアップ・プライバシー・Devpost説明用の技術記録

## 人間が行った製品・設計上の判断

- 毎回のトークン費用を発生させず、無料で継続利用できる端末内解析を選ぶ。
- 既存のCanvas解析を捨てず、衣装領域と背景除外を改善して精度を高める。
- 誤認識の影響が大きい衣装種類、シルエット、フォーマル度は自動推定しない。
- confidenceと警告を表示し、解析結果を決定ではなく入力候補として扱う。
- 利用者の確認・修正を保存前に求め、常に手入力を残す。
- 最終割り当てと理由は、説明可能で再現可能な既存の決定的アルゴリズムに残す。
- トップス、ボトムス、ネクタイや小物を単独で「1人分の衣装」と見なさず、ベース衣装と構成品をまとめた完成コーデをイベント候補にする。
- お気に入りを事前設定しなくても、登録済みの小物や上下別衣装を活用できる自動提案を作る。
- ブラウスやシャツは単品候補にせず、スカートやパンツが揃った状態だけを1人分の完成候補にする。
- 自動提案も外部AIや有料APIを使わず、既存属性の決定的ルールで無料実行する。
- 既存の所有衣装検索を新機能として数えず、Build Weekではお気に入りコーデ、検索・表示の統合、タグ保存修正として説明する。
- 完成コーデのために最適化エンジンを全面改修せず、互換レイヤーで1候補へ変換して既存の決定性を維持する。
- オンライン移行はD1、Worker、PWAの順に行い、旧Workerでは完成コーデだけを停止して単品提出を守る。
- GPT-5.6の役割を開発時の相談・実装・検証として正確に説明し、実行時API連携を主張しない。

## 本番デプロイ前チェック

- [x] `c334203`までの第1段階で、OpenAI API依存、OpenAI APIキー、モデル用secretがないことを確認
- [x] `c334203`までの第1段階で、型チェック、359件のテスト、ビルド、Worker型チェック、モバイル確認が成功
- [x] `d055d37`、`01bfb72`、`f85c67f`、`78befbe`、`23aff8a`を含む最終状態で型チェック、56ファイル・424件の全テスト、ビルドを再実行
- [x] 完成コーデ対象10ファイル・72件のテストとWorkerのTypeScriptチェックが成功
- [x] 自動提案、上下別衣装、候補多様性、構成品排他、写真revision、古い自動提案整理の主要6ファイル・82件とWorkerのTypeScriptチェックが成功
- [x] 新規`schema.sql`と追加マイグレーションのローカルSQLite実行が成功
- [ ] 所有衣装検索、お気に入りコーデ作成・編集・削除、複数写真表示を390×844と320×700で確認（in-app browserのセキュリティポリシーで未確認）
- [x] 単品、完成コーデ、途中失敗からの再試行、旧Worker時の安全な停止をモックで確認
- [x] 今回差分・作業ツリー・ビルド成果物・Git全履歴の秘密情報走査を再実行（新規秘密なし、旧履歴で1つのFirebase Web APIキー形式の値に関係する2変更は外部制限確認待ち）
- [ ] D1 / R2の既存bindingとCloudflare Workerの環境設定を確認
- [ ] `20260717_add_outfit_components.sql`を既存D1へ1回だけ適用
- [ ] Workerデプロイ後、`/api/health`の`apiVersion: "2026-07-18.2"`と`maxOutfitComponents: 3`を確認
- [ ] Cloudflare DashboardでR2 Public Accessが無効、または意図した設定であることを確認
- [ ] 旧Git履歴のFirebase Web APIキーについてGoogle側の制限とSecurity Rulesを確認
- [ ] 専用デモ参加者で、端末内解析・確認・コーデ作成・3スロット提出・最適化・公開結果を通し確認
- [ ] 公開URLでモバイル幅、PWA更新、既存単品イベント導線を確認
- [x] 安全な合成衣装による最終スクリーンショット／動画素材を取得し、人物情報、トークン、本番イベントが写っていないことを確認
- [ ] 公開リポジトリのライセンス表示を提出要件に合わせて人間が確定

OpenAI APIキーの設定はデプロイ条件ではありません。本番デプロイは、既存のCloudflare設定と上記の外部確認を完了し、最終ビルドが成功した後に行います。

## Devpost説明文・動画へ反映する技術的事実

- Ensemble AIは、複数人の衣装候補入力とグループ全体の調和を扱うCostume CoordinatorのBuild Week拡張である。
- GPT-5.6 SolをPM・相談・セキュリティ・製品判断、GPT-5.6 Lunaを実装担当、Sol / Lunaを検証・レビューに使用した。
- Codexでリポジトリ監査、実装、テスト、既存画面のブラウザ／モバイル検証、Git追跡まで一貫して実施した。新しいコーデUIの最終モバイル確認は公開前チェックとして区別している。
- 公開アプリの写真解析はCanvasとTypeScriptで端末内実行し、外部AI API、APIキー、従量課金を必要としない。
- 端末内解析が候補入力するのは主色、副色、トーン、保守的に判定できる柄である。
- 衣装種類、シルエット、フォーマル度は自動推定せず、利用者が確認・修正する。
- Build Week前から単品衣装の登録、所有衣装検索、オンライン提出、決定的最適化は存在していた。
- Build Weekで、スーツ＋ワイシャツ＋ネクタイなどを「お気に入りコーデ」として保存し、構成品を保った1つの完成コーデ候補へ変換する機能を追加した。
- Build Weekで、ブラウス、トップス、スカート、パンツの登録と、お気に入り未設定時に登録アイテムから完成コーデを自動提案する機能を追加した。
- 自動提案は色、トーン、柄、季節、タグの決定的ルールで端末内実行し、OpenAI API、外部AI、従量課金を必要としない。
- トップス、ボトムス、ネクタイ、蝶ネクタイ、小物単品はイベント候補にせず、単品で完成する衣装または完成コーデを1人につき1組選ぶ。
- 完成コーデが決まった場合は全構成品へ使用履歴を記録し、小物だけを別コーデで即時再利用する状態も防ぐ。
- 同じ小物を含む複数の仮想コーデも物理構成IDで排他し、グループ内の2人へ同時割当しない。
- 候補上限内でも構成品が異なる代替案を優先し、同じ小物へ候補が集中する状態を緩和する。
- オンライン提出ではD1に構成メタデータ、R2に構成順の写真を保存し、途中再送と旧単品データへ後方互換を持つ。
- 再提出で不要になった自動提案だけを、参加者認証と所有権を確認してR2 / D1から整理する。
- 最終的な衣装割り当てと理由は、既存の決定的最適化エンジンが生成する。GPT-5.6がアプリ実行時に衣装を決定するわけではない。
- Build Week前からある機能と、2026-07-13以降に追加・強化した機能をこの文書で区別している。
- OpenAI API連携や実行時GPT推論は、実装済み機能として主張しない。
- 有料課金、請求、審査期間だけのAPI切替は実装済み機能として主張しない。
