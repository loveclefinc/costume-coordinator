# Ensemble AI — Build Week 2026 技術記録

この文書は、OpenAI Build Week 2026における実装範囲、再現手順、検証結果、GPT-5.6とCodexの使い方を記録する技術資料です。申請時の紹介文ではなく、審査・監査・デモ準備で確認できる事実をまとめています。

## 担当と期間

- プロジェクト: Ensemble AI（既存のCostume Coordinatorを拡張）
- 対象部門: Apps for Your Life
- Build Week開始: 2026-07-13
- 提出期限: 2026-07-21 17:00 PDT（日本時間 2026-07-22 09:00）
- PM・相談担当: GPT-5.6 Sol
- 実装担当: GPT-5.6 Luna
- 検証・レビュー: GPT-5.6 Sol / Luna
- コア機能を構築したCodex task: このタスク（提出時に`/feedback`でSession IDを取得）

## GPT-5.6とCodexを使用した箇所

GPT-5.6は、このCodex task内の開発工程で使用しました。公開アプリの実行時にGPT-5.6やOpenAI APIを呼び出す構成ではありません。

| 担当 | 開発工程での役割 |
| --- | --- |
| GPT-5.6 Sol | PM、要件整理、Build Week要項の解釈、製品方針・プライバシー・セキュリティ判断、検証レビュー |
| GPT-5.6 Luna | 実装担当としてリポジトリ監査、設計、React / TypeScript / Workerの変更、テストと技術記録の作成 |
| Codex | ファイル調査・編集、コマンド実行、テスト、ビルド、ブラウザ／モバイル確認、Gitによる変更追跡 |
| 人間 | プロダクト目的、コストを発生させない実行方式、AIに最終決定を委ねない方針、公開範囲と提出内容の最終判断 |

申請文とデモ動画では、GPT-5.6が「アプリ内で写真を解析している」とは説明しません。正確な説明は、GPT-5.6 Sol / LunaをCodexで相談・実装・検証に使用し、その成果として無料の端末内解析を強化した、というものです。OpenAI API連携、実行時のモデル推論、モデルによる選定理由生成は実装・申請上の機能として主張しません。

Build Weekの公式案内はCodexとGPT-5.6の使用、およびデモ動画で両者の使い方を説明することを求めています。公式FAQもGPT-5.6の使用を必須としています。本プロジェクトでは、上記の開発工程がその使用箇所です。

- [Official Rules](https://openai.devpost.com/rules)
- [Build Week FAQ](https://openai.devpost.com/details/faqs)

## 2026-07-13より前から存在した機能

Git履歴上、Build Week直前のHEADは`694a5b1`（2026-06-27）です。次の機能はBuild Week開始前から存在していました。

- React / TypeScript / ViteによるPWA
- 個人ワードローブへの衣装写真、色、トーン、柄、衣装種類の登録と手入力
- ブラウザCanvasによる主色・副色・トーンのローカル解析
- イベントと衣装テーマの作成
- 招待URLからの参加、候補衣装のオンライン提出
- Cloudflare Workers / D1 / R2によるイベント、参加者、衣装属性、提出写真の一時保管
- テーマ、希望順位、使用履歴、グループ調和を使う決定的最適化
- 主催者向けの最適化スコアと選定理由
- PWAのオフライン閲覧、Google Drive / Dropboxへの任意バックアップ

## Build Weekで追加・強化した機能

- 参加者が招待フロー内から衣装写真を登録し、元の提出画面へ安全に戻れる専用導線
- Canvas解析の精度改善
  - 写真中央の衣装領域を優先
  - 四隅で共通する背景色、白・黒・暖色系ニュートラル背景を抑制
  - 近い色を量子化して主色候補の偏りを軽減
  - 色の主・副候補とトーンを既存フォームへ入力
- 画素の境界、方向性、分散、連結領域を使った保守的な柄候補
  - 対象は無地、ストライプ、チェック、ドット、その他
  - confidence、警告、判定困難項目を返す
  - 衣装種類、シルエット、フォーマル度は写真から推定しない
- 未解析、確認中、入力済み、要確認、失敗、手動入力、再確認のUI状態
- 入力候補を利用者が確認・修正してから保存するレビュー導線
- JPEG / PNG / WebP、5MB以下という画像選択時の検証
- オンライン提出用WorkerでのMIME、magic bytes、実データ量の再検証と上限付きstream読込
- R2画像レスポンスの`private, no-store`、`nosniff`、旧SVGメタデータの安全な扱い
- 管理者トークンをURLクエリへ含めず、ヘッダーで送るようにするクライアント修正
- 参加者向け結果画面への、既存の決定的最適化結果から作る短い選定理由
- 低confidence、画像処理失敗、未対応形式でも手入力を続けられるフォールバック
- プライバシーポリシーと利用規約への端末内解析・オンライン提出境界の明記
- 端末内解析UI、画像検証、参加導線、選定理由、Worker画像境界、既存最適化のテスト

## 実行時アーキテクチャ

```text
衣装写真を選択
  └─ ブラウザで形式・容量を確認
       └─ Canvasで端末内解析
            ├─ 主色 / 副色 / トーン
            └─ 保守的な柄候補 / confidence / 警告
                 └─ 既存フォームへ一時入力
                      └─ 利用者が確認・修正
                           └─ 保存・イベント候補として提出
                                └─ 既存の決定的最適化
                                     └─ 選定衣装と簡潔な決定的理由
```

端末内解析はブラウザのCanvasとTypeScriptの決定的な画素処理だけで完結します。写真解析のための外部AI通信、OpenAI API呼び出し、モデル推論、サーバー側解析はありません。したがってOpenAI APIキーやモデル実行用のデプロイ設定も必要ありません。

オンライン提出を選んだ場合は、利用者が保存した後に既存のCloudflare Worker経路で衣装属性をD1、提出写真をR2へ一時保存します。これはイベント共有機能の通信であり、写真解析のための送信ではありません。

最終的な衣装割り当ては、Build Week前からある決定的最適化エンジンが担当します。Build Weekで追加した短い理由も、そのエンジンの既存理由から最大3件を整形したもので、文章生成モデルは使用しません。同じ入力からは同じ割り当てと理由を再現できます。

## セットアップ

### PWA

```bash
pnpm install
pnpm dev
```

端末内の写真解析だけを試す場合、OpenAIアカウント、OpenAI APIキー、追加の環境変数は不要です。

オンライン提出もローカルで試す場合は、ルートの`.env`に既存WorkerのURLを設定します。

```dotenv
VITE_EVENT_API_URL=https://costume-coordinator-events.YOUR_ACCOUNT.workers.dev
```

`.env`やローカル用の変数ファイルはGitへ追加しません。

### Cloudflare Worker

```bash
cd workers/event-api
pnpm install
npx wrangler deploy
```

Workerはオンライン提出、参加者認証、D1 / R2への一時保存に使用します。OpenAI用secretの登録は不要です。Cloudflareへのデプロイ認証情報や既存bindingは、リポジトリ外の安全な設定で管理します。

## テストと検証

```bash
# ルート
pnpm check
pnpm test
pnpm build

# Worker
cd workers/event-api
pnpm exec tsc --noEmit

# 差分の空白・競合確認
git diff --check
```

重点的に確認する項目は次のとおりです。

- 主色抽出における中央衣装領域、背景候補の除外、色量子化
- 柄候補のconfidence、警告、判定困難時の保守的な戻り値
- 成功、低confidence、失敗、手動入力、再確認と保存前確認
- JPEG / PNG / WebP、空ファイル、5MB超過、MIMEとmagic bytesの不一致
- `Content-Length`がないchunked uploadでも上限を超えた時点で中断すること
- 端末内解析の写真を外部AIへ送らないというUI表示
- 参加者の招待トークンを保持した追加・復帰導線
- 選定理由の整形、保存、公開結果での復元
- 既存の決定的最適化への回帰がないこと
- 管理者トークンがURLへ入らないこと

2026-07-17の最終ローカル検証では、現行Vite/PWAの型チェック、53ファイル・359件の全テスト、本番ビルド、Worker型チェック、`git diff --check`が成功しています。モバイルは390×844と320×700で横スクロールがなく、主要フォーム操作が44px以上であることを確認しました。ビルドには既存のchunkサイズとdynamic/static import重複の警告がありますが、失敗ではありません。

## 3分未満のデモ手順

本番利用者の画像やイベントを使わず、専用イベントと個人を識別できない衣装写真を使います。

1. 主催者画面で「Build Week Demo」イベントを作り、色・柄・トーンのテーマを設定する。
2. 招待URLを別ブラウザまたはプライベートウィンドウで開き、デモ参加者名を登録する。
3. 「衣装写真を追加」からJPEG / PNG / WebPの写真を選ぶ。
4. 写真の選択後、主色・副色・トーン・判定可能な柄が端末内で候補入力される様子を見せる。
5. confidenceと警告を確認し、衣装種類やシルエットを利用者が選ぶ。候補の1項目を修正する。
6. 確認欄を選んで保存し、イベント提出画面へ戻る。
7. 2〜3人分の安全な候補を提出済みにする。
8. 主催者画面で既存の決定的最適化を実行する。
9. 選ばれた衣装と、テーマ・希望・調和に基づく短い選定理由を表示する。
10. 写真解析は端末内であり、外部AI APIや従量課金がなく、いつでも手入力できることを説明する。

デモの音声では、GPT-5.6 SolがPM・相談・レビュー、GPT-5.6 Lunaが実装担当であり、Codexが監査・コード編集・テスト・ブラウザ確認・Git作業を進めたことを説明します。アプリ実行時の画像処理と決定ロジックは決定的であることも明示します。

再現性のため、デモ前に専用イベントで全手順を通します。本番データの削除や全体リセットを行うデモ機能は追加していません。

## プライバシーとセキュリティ

- 写真解析は端末内で完結し、解析目的で外部AIサービスへ写真を送信しない。
- OpenAI APIを使用しないため、OpenAI APIキーを設定・配布・コミットしない。
- 解析候補はフォーム上の一時値であり、利用者が確認・修正して保存する。
- 解析に失敗しても既存フォームと手入力を無効化しない。
- ローカル利用では衣装データをIndexedDBに保存し、Google Drive / Dropbox同期は利用者が選んだ場合だけ行う。
- オンライン提出では、Cloudflare Workerが参加者・管理者トークンとイベント期限を確認する。
- オンライン提出写真はJPEG / PNG / WebPのMIME、magic bytes、空ファイル、実データ量をWorkerで再確認する。
- R2画像は認証付きWorker経路で配信し、レスポンスをprivateかつno-storeにする。公開バケットURLを前提にしない。
- オンライン提出データはイベントの保存期限まで一時保管し、期限後または管理者による削除時にD1 / R2から削除する。
- 画像、data URL、認証トークン、秘密情報をログへ出力しない。
- GitHubが公開される前提で、`.env`、`.dev.vars`、秘密鍵形式を継続的に走査する。

今回の差分、作業ツリー、ビルド成果物、Git全履歴を値を表示しない方式で走査し、OpenAI、GitHub、AWS、Google、Stripe、Slack形式の高確度な秘密鍵は検出されませんでした。

Build Week以前のGit履歴にはFirebase Web APIキー形式のクライアント識別子があります。Firebase Web APIキーは一般にブラウザへ配布される識別子ですが、安全性はキー単体ではなくGoogle Cloud側の制限とFirebase Security Rulesに依存します。本番公開前にHTTP referrer、利用API制限、Security Rulesを外部設定で確認し、意図しないProjectであれば無効化またはローテーションします。この値は技術記録やログへ転載しません。

## Codexが実装を加速した箇所

- 既存のイベント、参加者、R2、衣装モデル、最適化経路の並列監査
- Build Week前の機能と追加差分のGit履歴による切り分け
- 画素統計を使う端末内解析アルゴリズムとレビューUIの実装
- React、共有型、Cloudflare Workerをまたぐ導線とセキュリティ修正
- 異常系、保存境界、最適化回帰を含む自動テスト
- 実ブラウザでのモバイル幅、操作サイズ、コンソール確認
- 公開GitHubを前提とした秘密情報境界とGit履歴の監査
- デモ・セットアップ・プライバシー・Devpost説明用の技術記録

## 人間が行った製品・設計上の判断

- 毎回のトークン費用を発生させず、無料で継続利用できる端末内解析を選ぶ。
- 既存のCanvas解析を捨てず、衣装領域と背景除外を改善して精度を高める。
- 誤認識の影響が大きい衣装種類、シルエット、フォーマル度は自動推定しない。
- confidenceと警告を表示し、解析結果を決定ではなく入力候補として扱う。
- 利用者の確認・修正を保存前に求め、常に手入力を残す。
- 最終割り当てと理由は、説明可能で再現可能な既存の決定的アルゴリズムに残す。
- GPT-5.6の役割を開発時の相談・実装・検証として正確に説明し、実行時API連携を主張しない。

## 本番デプロイ前チェック

- [x] OpenAI API依存、OpenAI APIキー、モデル用secretがないことを確認
- [x] `pnpm check`、`pnpm test`、`pnpm build`が成功
- [x] WorkerのTypeScriptチェックが成功
- [x] モバイル幅で横スクロールと主要操作サイズを確認
- [x] 今回差分・作業ツリー・ビルド成果物・Git履歴に高確度な秘密鍵がないことを確認
- [x] 最終コミット前の実装一式に対して、テスト・ビルド・型チェック・秘密情報走査を再実行
- [ ] D1 / R2の既存bindingとCloudflare Workerの環境設定を確認
- [ ] Cloudflare DashboardでR2 Public Accessが無効、または意図した設定であることを確認
- [ ] 旧Git履歴のFirebase Web APIキーについてGoogle側の制限とSecurity Rulesを確認
- [ ] デモ用参加者で、端末内解析・確認・提出・最適化・公開結果を通し確認
- [ ] 公開URLでモバイル幅、PWA更新、既存イベント導線を確認
- [ ] 公開リポジトリのライセンス表示を提出要件に合わせて人間が確定

OpenAI APIキーの設定はデプロイ条件ではありません。本番デプロイは、既存のCloudflare設定と上記の外部確認を完了し、最終ビルドが成功した後に行います。

## Devpost説明文・動画へ反映する技術的事実

- Ensemble AIは、複数人の衣装候補入力とグループ全体の調和を扱うCostume CoordinatorのBuild Week拡張である。
- GPT-5.6 SolをPM・相談・セキュリティ・製品判断、GPT-5.6 Lunaを実装担当、Sol / Lunaを検証・レビューに使用した。
- Codexでリポジトリ監査、実装、テスト、ブラウザ／モバイル検証、Git追跡まで一貫して実施した。
- 公開アプリの写真解析はCanvasとTypeScriptで端末内実行し、外部AI API、APIキー、従量課金を必要としない。
- 端末内解析が候補入力するのは主色、副色、トーン、保守的に判定できる柄である。
- 衣装種類、シルエット、フォーマル度は自動推定せず、利用者が確認・修正する。
- 最終的な衣装割り当てと理由は、既存の決定的最適化エンジンが生成する。
- Build Week前からある機能と、2026-07-13以降に追加・強化した機能をこの文書で区別している。
- OpenAI API連携や実行時GPT推論は、実装済み機能として主張しない。
