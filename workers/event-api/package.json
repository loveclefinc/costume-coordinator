{
  "name": "costume-coordinator-event-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute costume-events --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute costume-events --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "~5.9.2",
    "wrangler": "^3.94.0"
  }
}
